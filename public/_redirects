# Intentionally empty.
# No root 404.html → Cloudflare Pages SPA mode serves index.html for client routes.
# public/static/404.html + public/assets/404.html → missing hashed assets return real 404
# (not index.html), which avoids MIME "text/html" on .js requests.
